SELECT
SELECT DISTINCT
SELECT COUNT()
WHERE + condition (normal operator like = & AND,OR and NOT)
SELECT +  ORDER BY (ASC & DESC)
INSERT INTO + VALUES
IS NULL & IS NOT NULL with WHERE
UPDATE & SET
DELETE FROM
SELECT TOP & LIMIT & ROWNUM
SELECT MIN() & MAX()
SELECT COUNT() & AVG() & SUM()
WHERE + LIKE (% & _)
WHERE + IN
WHERE + BETWEEN
Aliases by AS
JOIN ON (INNER & LEFT & RIGHT & FULL OUTER & SELF)
UNION
GROUP BY
HAVING + Counts instad of WHERE
WHERE EXISTS
WHERE + (ANY & ALL)
SELECT INTO
INSERT INTO SELECT
IFNULL
Comments -- & /* test */
